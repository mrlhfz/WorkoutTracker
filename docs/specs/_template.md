# SPEC: `<relative path from repo root, e.g. backend/src/services/workoutService.ts>`

## Purpose

<1-3 sentences: what this file is for and why it exists.>

## Exports / Props

<For backend: exported functions/objects and their signatures. For frontend components: the
props interface. For `types.ts` files: the shapes defined and what consumes them.>

## Behavior

<Key logic, edge cases, invariants a reader needs to know that aren't obvious from skimming the
code — allowlists, error handling, side effects, ordering assumptions.>

## Dependencies

<What this file imports from elsewhere in the repo (not npm packages, unless the dependency on
a specific package version/behavior is load-bearing), and what else in the repo depends on this
file.>
